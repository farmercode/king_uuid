// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("king_uuid", "for king_uuid support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("king_uuid", "enable king_uuid support", "no");

if (PHP_KING_UUID != "no") {
	EXTENSION("king_uuid", "king_uuid.c");
}

